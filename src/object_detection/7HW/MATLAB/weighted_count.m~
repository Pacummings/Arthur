function [bins] = weighted_count(X, W)
    % Where W is a column vector the same height as X
    bins = zeros(size(X));
    for r=1:size(X,1)
        for c=1:size(X,2)
            
        end
    end
end